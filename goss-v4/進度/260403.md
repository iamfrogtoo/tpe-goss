# 2026-04-03 工作進度

## 完成項目

### 1. OpenSky 認證帳號確認
- ✅ 確認 `bridge_v4.py` 使用 OAuth2 認證模式（非匿名）
- ✅ 確認 `credentials.json` 存在且包含有效的 `clientId` / `clientSecret`
  - clientId：`xinzhi-api-client`
- ✅ 認證帳號優勢：請求頻率限制較寬（1 秒一次 vs 匿名的 10 秒一次）

### 2. OpenSky 429 問題診斷
- ✅ 建立 `test_opensky.py` 進行連線測試
- ❌ 測試結果：認證帳號被 Rate Limit 封鎖（429），需等待 **29646 秒（約 8.2 小時）**
  - 回應 Header：`X-Rate-Limit-Retry-After-Seconds: 29646`
- ✅ 匿名模式正常（依 IP 計算，本機 IP 未被封鎖，回傳 33 架航班）
- **根本原因**：`bridge_v4.py` 每 2 秒呼叫一次 OpenSky，累積請求超過帳號配額上限

### 3. 三支程式碼診斷（bridge_v4 / track_landing / export_to_gsheets）
- ✅ 全面檢查三支腳本，找出「收不到資料」的問題：

| 問題 | 說明 |
|------|------|
| OpenSky 429 | 認證帳號被封 8 小時，資料進不來 |
| Google credentials 衝突 | `credentials.json` 是 OpenSky 格式，程式誤判並 return None |
| Spreadsheet ID 錯誤 | 填的是 CSV 公開連結（`/d/e/`），無法寫入 |
| track_landing 無資料 | 僅查 LOCAL/OPENSKY 來源，兩者都失效時無任何輸入 |

### 4. FlightAware AeroAPI：昨日降落航班查詢
- ✅ 建立 `fetch_yesterday_arrivals.py`
- ✅ 以「時間視窗滑動」方式翻頁（修正 cursor 機制的 400 錯誤）
- ✅ 成功抓取昨日（2026-04-02）桃園機場（RCTP）降落航班 **100 筆**
  - 查詢範圍：台灣時間 00:00 ~ 23:59（UTC+8）
  - 共跑 7 頁，每頁 15 筆
  - 資料儲存至 `yesterday_arrivals.json`

### 5. Google Sheets 匯出：完整流程建立
- ✅ 在 Google Cloud Console（專案：`Default Gemini Project`）啟用 **Google Sheets API**
- ✅ 建立 Service Account：`goss-sheets-writer@gen-lang-client-0703014541.iam.gserviceaccount.com`
- ✅ 下載 JSON 金鑰，存為 `google_service_account.json`
- ✅ 試算表共用給 Service Account（編輯者權限）
- ✅ 建立 `export_arrivals_to_gsheets.py`
  - 使用 `google-auth` + `google-api-python-client`
  - 自動偵測工作表名稱（實際為「工作表1」而非 "Sheet1"）
  - 清空後重新寫入資料（101 列 × 10 欄 = 1010 個儲存格）
  - 標題列設定藍底白字粗體樣式

### 6. 匯出結果
- ✅ **成功寫入 1010 個儲存格（100 筆 + 標題列）**
- ✅ 試算表連結：https://docs.google.com/spreadsheets/d/1aNXOaARvfu_08g5yjnxuQMIko6r1m5T8CW06oM365lc/edit

#### 匯出欄位
| 序號 | 航班號(IATA) | 航班號(ICAO) | 起飛機場(IATA) | 起飛機場名稱 | 實際落地(台灣) | 預定落地(台灣) | 機型 | 機尾號 | 航空公司 |

## 新增檔案

| 檔案 | 說明 |
|------|------|
| `test_opensky.py` | OpenSky 連線測試（OAuth2 Token + API 呼叫，含匿名模式對比） |
| `fetch_yesterday_arrivals.py` | FlightAware AeroAPI 抓取昨日 RCTP 降落航班（時間視窗翻頁） |
| `export_arrivals_to_gsheets.py` | 將昨日航班 JSON 匯出至 Google Sheets（Service Account 認證） |
| `google_service_account.json` | Google Service Account JSON 金鑰（勿上傳至 Git） |
| `yesterday_arrivals.json` | 昨日 100 筆降落航班原始資料備存 |

## 系統狀態

- ✅ 本地天線：正常運作
- ❌ OpenSky 認證模式：封鎖中（預計 8.2 小時後解封）
- ✅ OpenSky 匿名模式：可用（臨時替代）
- ✅ FlightAware AeroAPI：正常
- ✅ Google Sheets 匯出：正常（Service Account 認證）
- ⏸️ `bridge_v4.py` + `track_landing.py` 完整流程：待 OpenSky 解封後驗證

## 待解決問題

1. **OpenSky 請求頻率**：`bridge_v4.py` 每 2 秒請求一次，需降低為 10 秒以上，避免再次觸發 429
2. **track_landing 資料來源**：目前只讀 LOCAL/OPENSKY，應加入 FlightAware 備援
3. **export_to_gsheets.py 重構**：原始腳本的 credentials 衝突問題未修正，建議以 `export_arrivals_to_gsheets.py` 取代

## 下一步計劃

1. 修正 `bridge_v4.py` OpenSky 請求間隔（建議 ≥ 10 秒）
2. 將 `track_landing.py` 接上 FlightAware 資料源
3. 建立定時排程（每日自動抓取昨日降落航班並匯出 Sheets）
4. 將 `export_to_gsheets.py` 統一重構為 Service Account 認證方式
