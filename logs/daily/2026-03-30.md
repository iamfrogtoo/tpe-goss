# 2026-03-30 工作進度

## 今日完成工作

### 1. 入境航機抓取邏輯優化
- 修改了 `fetch_tpe_v4.py`，添加了 `actual_time` 和 `status` 字段的抓取和存储
- 实现了专门处理延误航班的逻辑，确保延误超过4小时的航班不会被遗漏
- 修改了数据库表结构，在 `source_airport` 和 `flight_schedule` 表中添加了 `actual_time` 和 `status` 列

### 2. FlightRadar24 App 轉跳功能
- 实现了点击航班号跳转到 FlightRadar24 的功能
- 支持手机端跳转到 FlightRadar24 App，电脑端跳转到网页
- 使用深度链接技术，确保在不同设备上的正确跳转

### 3. 中央資料庫架構
- 完成了分区式数据库架构的设计和实现
- 创建了 `live_traffic`、`flight_schedule` 等核心表
- 创建了 `source_airport`、`source_opensky`、`source_antenna`、`source_fr24` 等分区表
- 实现了数据融合引擎，将不同数据源的数据整合到 `live_traffic` 表

### 4. 前端頁面優化
- 创建了 `TPE GOSS 頁面、腳本與資料庫映射表.md`，详细记录了前端页面、后端脚本与数据库表之间的对应关系
- 启动了前端开发服务器，预览了页面效果
- 修复了 outbound 页面重复显示柜台编号的问题

## 遇到的問題

### 1. 亂碼問題
- **問題描述**：outbound 页面显示的航班状态文本出现乱码，如 "�Ǯ�ON TIME" 和 "�ɶ����SCHEDULE CHANGE"
- **處理過程**：
  - 尝试了多种编码处理方式，包括使用 TextDecoder 解码不同编码
  - 尝试过滤掉乱码字符
  - 但问题仍未完全解决
- **後續計畫**：需要进一步分析 CSV 数据的编码格式，可能需要在后端处理编码问题

### 2. OpenSky API 請求過於頻繁
- **問題描述**：测试过程中遇到 OpenSky API 429 错误（请求过于频繁）
- **處理過程**：
  - 在 `fetch_opensky_v4.py` 中添加了 API 调用频率控制
  - 实现了错误处理，当遇到 429 错误时暂停 60 秒
- **解決狀態**：已解决，现在系统会自动控制 API 调用频率

## 後續工作計畫

1. **解決亂碼問題**：进一步分析 CSV 数据的编码格式，在后端处理编码问题
2. **完善前端功能**：继续优化前端页面，添加更多功能
3. **整合其他数据源**：实现 FlightAware 和华航邮件数据的抓取和整合
4. **部署到生产环境**：将修改后的代码部署到 i5 服务器
5. **测试系统稳定性**：在生产环境中测试系统的稳定性和可靠性

## 技術備註

- **数据库架构**：采用分区式架构，每个数据源独立存储，避免写入冲突
- **数据融合策略**：本地天线数据优先，OpenSky 数据作为备份
- **API 调用控制**：实现了 API 调用频率控制，避免超过服务提供商的限制
- **前端技术**：使用 Next.js 框架，部署在 Vercel 平台

## 檔案修改清單

- `fetch_tpe_v4.py`：优化入境航班抓取逻辑
- `bridge_v4.py`：实现本地天线接管和其他资源补充功能
- `fetch_opensky_v4.py`：确保 OpenSky 数据抓取的稳定性
- `init_db.py`：修改数据库表结构，添加 `actual_time` 和 `status` 列
- `alter_db.py`：修改现有数据库表结构
- `app/outbound/page.tsx`：修复重复显示柜台编号的问题，尝试解决乱码问题
- `TPE GOSS 頁面、腳本與資料庫映射表.md`：创建页面、脚本与数据库映射表